MONGODB_URI=mongodb://localhost:27017/bus
PORT=4000
JWT_SECRET=replace-with-a-long-random-string

# --- Push notifications (Firebase Cloud Messaging) ---
# Optional. Without these the API boots fine but every push silently no-ops
# with one warning line, and the admin's Send notification page reports
# "Push is not switched on for this server".
#
# Pick ONE of the two forms.
#
# 1) Path to the service-account JSON downloaded from the Firebase console
#    (Project settings -> Service accounts -> Generate new private key):
# FIREBASE_SERVICE_ACCOUNT_PATH=/etc/bus-api/firebase-service-account.json
#
# 2) Or the three fields inline. Keep the literal \n escapes in the key —
#    they are converted to real newlines at load.
# FIREBASE_PROJECT_ID=bus-tracking-b644b
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@bus-tracking-b644b.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# --- Image storage ---
# Driver photos, complaint screenshots and the banner. Same switch as kareez's
# src/fileStorage:
#
#   STORAGE_DRIVER unset (or anything but s3)  files on this machine's disk
#     IMAGE_SERVER_PATH  absolute folder the files are written to
#     IMAGE_SERVER_URL   public base URL for them — this API serves the folder
#                        at /images. Must be reachable from the phone, so on a
#                        laptop use its LAN address, not localhost.
#
#   STORAGE_DRIVER=s3  files in an S3 bucket instead; IMAGE_SERVER_* unused
#     AWS_REGION, AWS_S3_BUCKET, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY
#
# Photos written before storage existed are base64 in MongoDB and still show.
IMAGE_SERVER_PATH=/absolute/path/to/bus/api/uploads
IMAGE_SERVER_URL=http://192.168.1.48:4000/images

# STORAGE_DRIVER=s3
# AWS_REGION=ap-south-1
# AWS_S3_BUCKET=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
